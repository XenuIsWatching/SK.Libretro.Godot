## SystemManager — no longer needed.
## Each RetroSystem now owns its own Libretro node and Wrapper instance,
## so multiple systems can run simultaneously without coordination.
## This file is kept for reference only and is not loaded as an autoload.
extends Node
